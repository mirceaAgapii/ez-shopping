export class User {
    role: string | null = '';
    username: string | null = '';

    constructor(username?: string) {}
        if(username: string | null) {
            this.username = username;
        }
   }
  