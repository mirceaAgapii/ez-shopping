export class User {
    id: string = '';
    role: string | null = '';
    username: string | null = '';
    password: string | null = '';
    email: string | null = '';

    constructor(
        id: string = '',
        role: string | null = '',
        username: string | null = '',
        password: string | null = '',
        email: string | null = '') {}

   }
